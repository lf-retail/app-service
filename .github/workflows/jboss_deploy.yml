name: jboss-caller-new

on:
 workflow_dispatch:
 
env: 
  Workflow2_PAT_TOKEN_GITHUB: ${{ secrets.Workflow2_PAT_TOKEN_GITHUB }}
  
jobs:
  caller-jboss-deploy:
     runs-on: ubuntu-latest
     steps:
      - name: Trigger Caller Workflowii
        uses: actions/checkout@v3
     
      - name: Run Callee Workflow
        run: |
          curl -X POST \
          -H "Accept: application/vnd.github.v3+json" \
          -H "Authorization: Bearer ${{ secrets.Workflow2_PAT_TOKEN_GITHUB }}" \
          https://github.com/lf-common-repo/shared-workflow/.github/workflows/Jboss.yml \
          -d '{"ref":"main"}'
